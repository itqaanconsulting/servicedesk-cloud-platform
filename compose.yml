services:
  ticket-database:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: tickets
      POSTGRES_USER: servicedesk
      POSTGRES_PASSWORD: servicedesk
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U servicedesk -d tickets"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - ticket-data:/var/lib/postgresql/data

  ticket-service:
    build:
      context: services/ticket-service
    ports:
      - "8181:8081"
    environment:
      SERVER_PORT: 8081
      DB_URL: jdbc:postgresql://ticket-database:5432/tickets
      DB_USERNAME: servicedesk
      DB_PASSWORD: servicedesk
      TECHNICIAN_SERVICE_URL: http://technician-service:8082
    depends_on:
      ticket-database:
        condition: service_healthy
      technician-service:
        condition: service_started

  technician-database:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: technicians
      POSTGRES_USER: servicedesk
      POSTGRES_PASSWORD: servicedesk
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U servicedesk -d technicians"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - technician-data:/var/lib/postgresql/data

  technician-service:
    build:
      context: services/technician-service
    ports:
      - "8082:8082"
    environment:
      SERVER_PORT: 8082
      DB_URL: jdbc:postgresql://technician-database:5432/technicians
      DB_USERNAME: servicedesk
      DB_PASSWORD: servicedesk
    depends_on:
      technician-database:
        condition: service_healthy

  notification-service:
    build:
      context: services/notification-service
    ports:
      - "8083:8083"
    environment:
      SERVER_PORT: 8083

volumes:
  ticket-data:
  technician-data:
