apiVersion: v1
kind: Service
metadata:
  name: technician-service
  namespace: servicedesk
  labels:
    app: technician-service
spec:
  selector:
    app: technician-service
  ports:
    - name: http
      port: 8082
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: technician-service
  namespace: servicedesk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: technician-service
  template:
    metadata:
      labels:
        app: technician-service
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: technician-service
          image: servicedesk-cloud-platform-technician-service:latest
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8082
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: TECHNICIAN_DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: database-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-credentials
                  key: password
            - name: MANAGEMENT_TRACING_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: MANAGEMENT_TRACING_ENABLED
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 15
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 750m
              memory: 768Mi
