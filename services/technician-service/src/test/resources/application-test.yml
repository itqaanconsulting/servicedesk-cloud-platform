spring:
  datasource:
    url: jdbc:h2:mem:technicians;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration

management:
  tracing:
    enabled: false
